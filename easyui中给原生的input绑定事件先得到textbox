$("#formTimeDiclare_Hours").numberbox("textbox").bind('blur', function () { alert("事件来了！") });
numberbox开展来textbox,

textbox('textbox').bind('keydown', function(e){
	if (e.keyCode == 13){	// when press ENTER key, accept the inputed value.
		t.textbox('setValue', $(this).val());
	}
});
